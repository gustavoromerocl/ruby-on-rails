<div class="large-container center-block">
    <h1>Mis artículos</h1>

    <%= render @articles %>
</div>



<!--
Si se crea un archivo parcial con el nombre del modelo, la convención de rails reconoce automaticamente que tiene que iterar y mostrar la coleccion

<% @articles.each do |article| %>
    <div class="card" style="padding: 15px; max-width: 300px;">
        <h3 class="text-uppercase"> 
            <%= article.title%>
        </h3>
        <p class="text-secondary"><%= article.created_at%></p>
        <%= link_to "Ver más", article %>
    </div>
<% end %>
-->